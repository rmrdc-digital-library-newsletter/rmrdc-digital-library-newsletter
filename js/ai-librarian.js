const chatMessages=document.getElementById('chatMessages'),chatForm=document.getElementById('aiChatForm'),chatInput=document.getElementById('aiChatInput');
function addMessage(role,text){const div=document.createElement('div'); div.className='chat-bubble '+role; div.innerHTML=`<strong>${role==='assistant'?'AI Librarian':'You'}</strong><p>${text}</p>`; chatMessages.appendChild(div); chatMessages.scrollTop=chatMessages.scrollHeight; return div;}
async function askAILibrarian(question){if(!window.db)throw new Error('Supabase is not connected. Check js/config.js.'); const {data,error}=await window.db.functions.invoke('ai-librarian',{body:{question}}); if(error)throw error; return data?.answer||'I could not generate an answer.';}
chatForm.addEventListener('submit',async e=>{e.preventDefault(); const q=chatInput.value.trim(); if(!q)return; addMessage('user',q); chatInput.value=''; const pending=addMessage('assistant','Searching publication records and preparing a grounded answer...'); try{pending.querySelector('p').textContent=await askAILibrarian(q);}catch(error){pending.querySelector('p').textContent=error.message||'AI Librarian is not configured. Deploy the ai-librarian Edge Function.';}});